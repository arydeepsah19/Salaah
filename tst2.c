#include <stdio.h>

int main() {
    int a,b;
    scanf("%d",&a);
    scanf("%d",&b);
    if (a>b)
    {
        printf("Hello World!");
    }
    
    else if (a<b)
    {
        printf("Not so Great!");
    }
    else
    {
        printf("Move on!");
    }
    
    return 0;
}
